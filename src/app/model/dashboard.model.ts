export interface Dashboard {
  userCount: number;
  investment: string;
  payout: string;
  pf: string;
  pfInterest: string;
  safeKeeping: string;
  emergency_loan_completed: string;
  emergency_loan_active: string;
  cycle_pending: string;
  cycle: string;
  loan_completed: string;
  loan_active: string;
  loan_paid_user: string;
}
